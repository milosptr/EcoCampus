// React Native doesn't use className, so these utilities are not needed
// This is a stub for compatibility with code that might import this
export function cn(...inputs: any[]) {
  // No-op for React Native
  return "";
}
