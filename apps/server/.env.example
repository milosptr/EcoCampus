# Server
PORT=3000


# =============================================================================
# DATABASE CONNECTION (PostgreSQL via Supabase)
# =============================================================================
# Connection pooler URL - Used by Prisma for regular queries
# Get this from: Supabase Dashboard > Project Settings > Database > Connection Pooling > Transaction mode
# Format: postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres
DATABASE_URL=""

# Direct connection URL - Required for Prisma migrations (bypasses connection pooler)
# Get this from: Supabase Dashboard > Project Settings > Database > Connection string > URI format
# Format: postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
DIRECT_URL=""

# =============================================================================
# SUPABASE SERVICES (Auth, Storage, Realtime)
# =============================================================================
# Supabase API endpoint - Used by Supabase JS client for authentication and other services
# Get this from: Supabase Dashboard > Project Settings > Data API > Project URL
# Format: https://[PROJECT-REF].supabase.co
SUPABASE_URL=""

# Service role key - Admin API key with elevated privileges (bypasses Row Level Security)
# ⚠️ KEEP SECRET - Only use on backend, never expose to client
# Get this from: Supabase Dashboard > Project Settings > API keys > Legacy anon, service_role API keys > service_role
SUPABASE_SERVICE_ROLE_KEY=""

# =============================================================================
# AUTHENTICATION
# =============================================================================
# Supabase JWT Secret - Used to verify JWT tokens issued by Supabase
# Get this from: Supabase Dashboard > Project Settings > API > JWT Settings > JWT Secret
# ⚠️ DO NOT generate your own - use the secret provided by Supabase
# Format: Base64-encoded string (typically ~64 characters)
SUPABASE_JWT_SECRET="your_supabase_jwt_secret_from_dashboard"
